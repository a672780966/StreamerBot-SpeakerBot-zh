# Streamer.bot / Speaker.bot 四语言汉化包

本项目对 **Streamer.bot** 和 **Speaker.bot** 提供四种界面语言：**简体中文 (zh-CN)**、**日本語 (ja-JP)**、**한국어 (ko-KR)**、**Español (es-ES)**。

采用**注入式**方案：不改动 exe、不破坏数字签名，通过 .NET `AppDomainManager` 机制在程序启动时自动加载旁置的 `ZhInject.dll`，把界面文本替换为所选语言。完全可逆，一键还原、一键切换语言。

## 目录结构

```
汉化多语言版\
├─ 安装.ps1          安装脚本（语言菜单 / -Lang 参数 / 自动启动）
├─ 切换语言.ps1      语言切换脚本（部署对应映射并重启程序）
├─ 还原.ps1          还原脚本（删除全部汉化文件+恢复配置，还原英文）
├─ 使用说明.md       本文档
├─ src\
│  └─ ZhInject.dll   注入器（语言无关注入器，四种语言共用同一 DLL）
├─ Streamer.bot\
│  ├─ zh-CN\ZhMap.tsv   简体中文映射（1089 条）
│  ├─ ja-JP\ZhMap.tsv   日本語映射（1089 条）
│  ├─ ko-KR\ZhMap.tsv   한국어映射（1089 条）
│  └─ es-ES\ZhMap.tsv   Español映射（1089 条）
└─ Speaker.bot\
   ├─ zh-CN\ZhMap.tsv   简体中文映射（181 条）
   ├─ ja-JP\ZhMap.tsv   日本語映射（181 条）
   ├─ ko-KR\ZhMap.tsv   한국어映射（181 条）
   └─ es-ES\ZhMap.tsv   Español映射（181 条）
```

## 安装

1. 右键 `安装.ps1` → **使用 PowerShell 运行**，按提示选择语言（`1) 简体中文 2) 日本語 3) 한국어 4) Español`）。
   - 或直接指定语言跳过菜单：
     - `powershell -ExecutionPolicy Bypass -File 安装.ps1 -Lang ja-JP`
     - `-Lang` 可选 `zh-CN` / `ja-JP` / `ko-KR` / `es-ES`
2. 安装完成后脚本**自动启动**两个程序（可用 `-NoStart` 跳过）。
3. 稍等片刻界面即为所选语言。

安装脚本会自动：
- 关闭正在运行的程序
- 备份原 `*.exe.config` → `*.exe.config.bak`
- 在 `<runtime>` 注入 AppDomainManager 两行配置
- 部署 `ZhInject.dll` + 所选语言的 `ZhMap.tsv`，写入 `ZhMode.txt=translate` 与 `ZhLang.txt=<语言>`

## 切换语言

- 右键 `切换语言.ps1` → 选择新语言，脚本会部署对应映射并**自动重启**程序使新语言生效。
- 或指定语言：`powershell -ExecutionPolicy Bypass -File 切换语言.ps1 -Lang es-ES`
- 当前生效语言记录在程序目录的 `ZhLang.txt`。

## 还原英文

关闭程序后运行 `还原.ps1`，将：
- 删除 `ZhInject.dll / ZhMap.tsv / ZhMode.txt / ZhLang.txt / ZhApply.log / ZhDebug.txt`
- 从 `.bak` 恢复原始配置（无备份时直接移除注入行）

## 翻译机制

- `ZhMap.tsv`：翻译映射，格式 `英文<TAB>译文`，UTF-8。首行 `# english	<语言>` 为表头。
- **占位符匹配**：含 `{N}` 的条目（如 `Actions ({0})`）自动编译为正则，运行时把控件值
  `Actions (3)` 匹配并替换占位符。仅对纯 ASCII 原文生效，避免二次改写已翻译文本。
- **保留原文**：译文与原文相同（如 `Discord`、`1.0.7`、`ms`）表示"保持英文不翻译"；
  译文为空表示"抑制该词翻译"（如 `Supporter`）。
- **键集合一致性**：四种语言映射与中文源映射键集合完全一致（Streamer.bot 1089 条 / Speaker.bot 181 条），
  逐一校验无缺失、无多余；`{N}` 与 `%变量%` 占位符逐一校验保持完整。
- `ZhMode.txt`：`translate` = 启用汉化；`capture` = 采集模式（抓取界面文本生成映射）。
- `ZhApply.log`：运行日志，含 `map loaded: N patterns: M`、`applied: X -> Y`（精确匹配）、
  `appliedp: X -> Y`（占位符匹配），可查看翻译生效情况。
- 程序启动时加载映射，每 2 秒扫描一次已打开窗口，对**静态界面标签**做翻译；动态数据不受影响。
- **注意**：修改映射或切换语言后需**重启程序**生效（映射热加载无效，切换脚本会自动重启）。

## 覆盖统计

| 语言 | Streamer.bot | Speaker.bot |
|---|---|---|
| zh-CN 简体中文 | 1079 已译 / 9 保留英文 / 1 抑制 | 166 已译 / 15 保留英文 |
| ja-JP 日本語 | 1077 已译 | 164 已译 |
| ko-KR 한국어 | 1075 已译 | 165 已译 |
| es-ES Español | 1070 已译 | 162 已译 |

（已译数不含保留英文与抑制条目；映射键集合各语言完全一致。）

## 已验证结果

运行时逐语言验证均通过：`applied` 生效、`seterr = 0`。

| 步骤 | 结果 |
|---|---|
| 安装 ja-JP（自动启动） | ✓ 映射 SHA256 与包内一致，界面应用日语 |
| 切换 ko-KR（自动重启） | ✓ 界面应用韩语 |
| 切换 es-ES（自动重启） | ✓ 界面应用西班牙语 |
| 切换 zh-CN（自动重启） | ✓ 界面应用中文 |
| 还原英文 | ✓ 全部汉化文件删除、配置还原 |

## 翻译延迟说明

程序启动后界面英文需要短暂等待才变为所选语言，属**注入式方案固有机制**：

- 注入器启动后 **0.5 秒**首次扫描已打开窗口，此后**每 1 秒**重新扫描一次（`ZhInject.cs` 定时器参数）。
- 英文显示时长 = 程序窗口创建完成前的时间 + 最长 1 秒扫描间隔。
- 实测（本机）：Speaker.bot 约 **2.1 秒**、Streamer.bot 约 **9.1 秒** 出现首个翻译。
  Streamer.bot 剩余延迟主要来自其 WPF 主窗口自身初始化（约 9 秒），属程序启动开销，非注入器瓶颈。
- 窗口就绪后，全部静态文本在下一个扫描周期（≤1 秒）内统一切换完成。
- 打开**新窗口/新页面**时同样最多等待 1 秒即被翻译。

## 说明与限制

- 未翻译条目为**品牌/集成名**（Twitch、Discord、OBS Studio、Elgato*、Patreon 等）、
  **技术术语/单位**（ms、MIDI、TLS）、**数字/GUID/格式串**（1.0.7、`(0/0)`、`{0}/{1}`），按设计保留英文。
- **已知限制**：主窗口标题在应用重新绑定数据后可能被还原为英文（数据绑定优先级高于本地值），属应用侧行为。
- 专有名词与品牌保留原文；程序更新后新增文本可能不完整，重新运行采集模式可补充。